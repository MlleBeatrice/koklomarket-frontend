# URL du backend PHP. En local avec `php -S localhost:8000 -t backend`, laissez la valeur par défaut.
# En production, c'est l'URL réelle d'AlwaysData (ex: https://beatrice.alwaysdata.net).
VITE_API_URL=https://beatrice.alwaysdata.net
